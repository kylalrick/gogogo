local Players = game:GetService("Players")local RunService = game:GetService("RunService")local UserInputService = game:GetService("UserInputService")local LocalPlayer = Players.LocalPlayerlocal _Active, _Visuals, _StaffDetect = false, false, falselocal _Size, _Trans = 15, 0.7-- GUI Principallocal ScreenGui = Instance.new("ScreenGui", game:GetService("CoreGui"))ScreenGui.ResetOnSpawn = falselocal MainFrame = Instance.new("Frame", ScreenGui)MainFrame.Size = UDim2.new(0, 220, 0, 320)MainFrame.Position = UDim2.new(0.5, -110, 0.5, -160)MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)MainFrame.Visible = trueMainFrame.Active = trueMainFrame.Draggable = trueInstance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)-- Bouton d'ouverture Mobilelocal OpenBtn = Instance.new("TextButton", ScreenGui)OpenBtn.Size = UDim2.new(0, 50, 0, 50)OpenBtn.Position = UDim2.new(0, 10, 0.5, -25)OpenBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 255)OpenBtn.Text = "MENU"OpenBtn.TextColor3 = Color3.new(1, 1, 1)OpenBtn.Font = Enum.Font.GothamBoldInstance.new("UICorner", OpenBtn).CornerRadius = UDim.new(1, 0)OpenBtn.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)local Title = Instance.new("TextLabel", MainFrame)Title.Size = UDim2.new(1, 0, 0, 35)Title.Text = "BADDIES LITE"Title.Font = Enum.Font.GothamBoldTitle.TextColor3 = Color3.new(1, 1, 1)Title.TextSize = 14Title.BackgroundTransparency = 1local function createBtn(name, pos, color) local btn = Instance.new("TextButton", MainFrame) btn.Size = UDim2.new(0, 180, 0, 35) btn.Position = pos btn.BackgroundColor3 = color or Color3.fromRGB(45, 45, 50) btn.Text = name btn.Font = Enum.Font.GothamSemibold btn.TextColor3 = Color3.new(1, 1, 1) btn.TextSize = 13 Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6) return btnendlocal HitboxBtn = createBtn("Hitbox: OFF", UDim2.new(0, 20, 0, 45))local VisualBtn = createBtn("Visuals: OFF", UDim2.new(0, 20, 0, 90))local StaffBtn = createBtn("Staff Detect: OFF", UDim2.new(0, 20, 0, 135))-- Sliders FIXÉSlocal function createSlider(name, pos, min, max, default) local label = Instance.new("TextLabel", MainFrame) label.Size = UDim2.new(1, 0, 0, 20) label.Position = pos label.Text = name .. ": " .. default label.Font = Enum.Font.GothamBold label.TextColor3 = Color3.new(1,1,1) label.TextSize = 12 label.BackgroundTransparency = 1 local sliderBtn = Instance.new("TextButton", MainFrame) sliderBtn.Size = UDim2.new(0, 180, 0, 15) sliderBtn.Position = pos + UDim2.new(0, 20, 0, 22) sliderBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 45) sliderBtn.Text = "" sliderBtn.AutoButtonColor = false Instance.new("UICorner", sliderBtn) local fill = Instance.new("Frame", sliderBtn) fill.Size = UDim2.new((default-min)/(max-min), 0, 1, 0) fill.BackgroundColor3 = Color3.fromRGB(0, 120, 255) Instance.new("UICorner", fill) local dragging = false local function update(input) local inputPos = input.Position.X local sliderPos = sliderBtn.AbsolutePosition.X local sliderWidth = sliderBtn.AbsoluteSize.X local percentage = math.clamp((inputPos - sliderPos) / sliderWidth, 0, 1) fill.Size = UDim2.new(percentage, 0, 1, 0) local val = min + (percentage * (max - min)) if max > 1 then val = math.floor(val) else val = tonumber(string.format("%.1f", val)) end label.Text = name .. ": " .. val return val end sliderBtn.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = true local val = update(input) if name == "Taille" then _Size = val else _Trans = val end end end) UserInputService.InputChanged:Connect(function(input) if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then local val = update(input) if name == "Taille" then _Size = val else _Trans = val end end end) UserInputService.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end end) return labelendcreateSlider("Taille", UDim2.new(0, 0, 0, 185), 2, 50, 15)createSlider("Transparence", UDim2.new(0, 0, 0, 245), 0, 1, 0.7)-- Logique BoutonsHitboxBtn.MouseButton1Click:Connect(function() _Active = not _Active HitboxBtn.Text = _Active and "Hitbox: ON" or "Hitbox: OFF" HitboxBtn.BackgroundColor3 = _Active and Color3.fromRGB(0, 180, 100) or Color3.fromRGB(45, 45, 50)end)VisualBtn.MouseButton1Click:Connect(function() _Visuals = not _Visuals VisualBtn.Text = _Visuals and "Visuals: ON" or "Visuals: OFF" VisualBtn.BackgroundColor3 = _Visuals and Color3.fromRGB(0, 120, 255) or Color3.fromRGB(45, 45, 50)end)StaffBtn.MouseButton1Click:Connect(function() _StaffDetect = not _StaffDetect StaffBtn.Text = _StaffDetect and "Staff: ACTIVE" or "Staff: OFF" StaffBtn.BackgroundColor3 = _StaffDetect and Color3.fromRGB(255, 80, 0) or Color3.fromRGB(45, 45, 50)end)-- Boucle HitboxRunService.RenderStepped:Connect(function() for _, p in pairs(Players:GetPlayers()) do if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then local hrp = p.Character.HumanoidRootPart if _Active then hrp.Size = Vector3.new(_Size, _Size, _Size) hrp.Transparency = _Visuals and _Trans or 1 hrp.CanCollide = false else hrp.Size = Vector3.new(2, 2, 1) hrp.Transparency = 1 end end endend)
